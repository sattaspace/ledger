---
/**
 * Budgets list page — /dashboard/budgets
 */

import DashboardLayout from "@/layouts/DashboardLayout.astro";
import BudgetsPage from "@/components/vue/budgets/BudgetsPage.vue";
---

<DashboardLayout title="Budgets — Satta Ledger">
  <BudgetsPage client:only="vue" />
</DashboardLayout>

<script>
  const pageTitle = document.getElementById("page-title");
  if (pageTitle) pageTitle.textContent = "Budgets";
</script>
