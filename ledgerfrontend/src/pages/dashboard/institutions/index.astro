---
/**
 * Institutions list page — /dashboard/institutions
 */

import DashboardLayout from "@/layouts/DashboardLayout.astro";
import InstitutionsPage from "@/components/vue/institutions/InstitutionsPage.vue";
---

<DashboardLayout title="Institutions — Satta Ledger">
  <InstitutionsPage client:only="vue" />
</DashboardLayout>

<script>
  const pageTitle = document.getElementById("page-title");
  if (pageTitle) pageTitle.textContent = "Institutions";
</script>
